@page "/"
@inject EPTools.Services.DiscordWebhookService dsw

<button @onclick="@(async () => await Post())">Post</button>

@code {
    private async Task Post()
    {
        await dsw.SendWebhook();
    }
}